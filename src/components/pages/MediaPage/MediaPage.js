import React from "react";
import Media from "../../template/Media/Media";

const wrapperStyles = {
  maxWidth: "100%",
  margin: "auto",
};

const MediaPage = () => (
  <div style={wrapperStyles}>
    <Media />
  </div>
);

export default MediaPage;
