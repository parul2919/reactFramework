import React from "react";
import FormEx from "../../template/FormEx/FormEx";

const wrapperStyles = {
  maxWidth: "100%",
  margin: "auto",
};

const FormExPage = () => (
  <div style={wrapperStyles}>
    <FormEx />
  </div>
);

export default FormExPage;
